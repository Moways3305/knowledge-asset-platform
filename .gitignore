# dependencies
node_modules/
.pnpm-store/

# build outputs
dist/
build/
.next/
.out/

# env / secrets
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# local temporary files
.codex_tmp/
.claude_tmp/

# python / backend
# 本地受控文件存储（dev/test 上传字节落盘处，不入库）
_local_storage/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
coverage.xml
# 前端 vitest v8 覆盖率输出目录（lcov / text）。
coverage/
*.sqlite3
postgres-data/
pgdata/

# claude code
.claude/

# docs — internal collaboration docs stay private; only published guides under
# docs/deployment/ are tracked. To publish another docs subdir, add its own "!docs/<dir>/".
docs/*
!docs/deployment/
# 版本化的 API 契约（OpenAPI spec + 系统能力清单），供前后端一致性校验与 CI 漂移检测。
!docs/api/

#claude
CLAUDE.md